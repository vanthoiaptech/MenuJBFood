export const domain = 'https://jmfood.herokuapp.com';

export const restaurantImageUrl = imageName =>
  `${domain}/images/restaurants/${imageName}`;

export const foodImageUrl = imageName => `${domain}/images/foods/${imageName}`;
